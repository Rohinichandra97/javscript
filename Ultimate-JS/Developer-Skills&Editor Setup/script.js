'use strict'; // if we give muitiple lines if spaces, it will automatically changes,
const x = 3;

if (x === 3) console.log(x); //automatically add space after if

const calcAge = birthYear => 2023 - birthYear; // if we write this configuration in prettierrc file "arrowParens": "avoid" , it wil avoid paranthasis for arrow function,
console.log(x); // using user snippets section under  prefereneces, we can create these user snippets, when i type "cl" and press enter it's automatically print "console.log"
console.log('hello, npm');
console.log('working');
